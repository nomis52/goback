listener:
  addr: ":8080"
cron:
  - workflows:
      - "power-on"
      - "combined-backup"
      - "power-off"
    schedule: "5 4 * * *"
state_dir: "./state"
log_level: "info"
workflow_config: "./config.yaml"
